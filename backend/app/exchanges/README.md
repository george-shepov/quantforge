Exchange environments are selected with `QUANTFORGE_<EXCHANGE>_ENV` and must be one of the explicitly supported values in `environment.py`.

The current milestone intentionally exposes no order-submission API. Testnet and demo endpoints are configured for market-data validation and future gated execution work only.
